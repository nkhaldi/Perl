#!/usr/bin/env perl


# Напишите программу, которая работает как cat,
# но выводит порядок выходных строк.
# (В некоторых системах существует такая утилита с именем tac.)

use 5.018;
use warnings;

print reverse <>;
